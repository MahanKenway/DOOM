# برنامهٔ RetroPlay

## هدف

تبدیل صفحهٔ تک‌بازی DOOM به یک هاب منتخب برای تجربه‌های ساخته‌شده با WAD؛ با اجرای فوری Freedoom، مسیر روشن برای واردکردن IWAD/PWAD شخصی و یک catalog قابل‌گسترش برای آثار اکشن، وحشت، اکتشاف و تجربی.

## ریسک‌های اصلی

### ۱. حفظ جریان اجرای WASM هنگام بازطراحی پوسته

- **چرایی جداسازی:** شناسه‌های فعلی انتخاب WAD و transition بین loading، picker و game screen به bootstrap موتور متصل‌اند.
- **رویکرد:** شناسه‌های سازگار (`wad-picker`، `btn-freedoom`، `wad-upload`، `wad-drop-zone`) حفظ می‌شوند؛ هاب جدید فقط لایهٔ تعامل و ارائه را تغییر می‌دهد.
- **تأیید:** Freedoom از کارت منتخب و از دکمهٔ اصلی اجرا شود؛ آپلود یک IWAD یا ترکیب IWAD + PWAD همچنان وارد loading و game screen شود.

### ۲. خوانایی رابط پنل‌های لایه‌ای روی دسکتاپ و موبایل

- **چرایی جداسازی:** پنل‌های نیمه‌شفاف و grid مهندسی در عرض کم می‌توانند به عناصر فشرده یا نامرتب تبدیل شوند.
- **رویکرد:** layout اصلی بر CSS grid منعطف است؛ پنل فیلتر در موبایل پایین صفحه می‌رود و کارت‌ها یک‌ستونه می‌شوند.
- **تأیید:** در 1440px، 1024px و 390px هیچ هم‌پوشانی، متن بریده یا کنترل غیرقابل‌دسترسی نباشد.

## ساخت اصلی

- هدر RetroPlay شامل Discover، Collections و Library.
- پنل Featured برای اجرای Freedoom و مشاهدهٔ metadata.
- catalog چهارگانه شامل Action، Horror، Exploration و Experimental با metadata و وضعیت اجرا.
- فیلترهای ژانر/فرمت و جست‌وجوی فوری در مرورگر.
- Library محلی بر مبنای localStorage برای بازی‌های pin‌شده.
- پنل Bring Your Own WAD که اجرای یک IWAD و تا سه PWAD را به جریان فعلی متصل می‌کند.
- loading، HUD، pause و settings هم‌سبک با زبان بصری RetroPlay.

## دارایی‌های لازم

- یک تصویر مرجع رابط برای تثبیت زبان بصری.
- یک تصویر header/cover تاریک و فضایی برای بازی منتخب Freedoom.
- سه cover تصویری برای مجموعه‌های Horror، Exploration و Experimental؛ هرکدام بدون متن و قابل استفاده داخل کارت.

## معیارهای تأیید

- ظاهر خانه حس رابط حرفه‌ای PSP-الهام با پنل‌های فضایی، grid ریز و accent لیمویی کنترل‌شده داشته باشد.
- Discover، Collections، جست‌وجو، فیلتر ژانر و Library قابل‌استفاده باشند.
- کارت Freedoom بازی را اجرا کند و کارت‌های catalog غیرباندل، مسیر واردکردن WAD شخصی را شفاف باز کنند.
- اجرای قبلی Freedoom/WASM و ورود IWAD/PWAD شکسته نشود.
- هیچ خطای JavaScript در کنسول نباشد و تمامی مسیرهای UI در مرورگر قابل مشاهده باشند.
